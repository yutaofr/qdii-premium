# 期货基差复审处理记录（f237d46）

日期：2026-09-18。基线：`f237d46a8d83c8d3030f09fc71e9ebb047bced66`。依据：复审意见与整改计划
[docs/plans/2026-09-18-futures-basis-review-remediation.md](../../docs/plans/2026-09-18-futures-basis-review-remediation.md)。

## 结论

接受复审对 f237d46 的全部判定。本次交付的是**探索性工具整改与产品披露**，不是亚洲时点误差的验证：

- **亚洲决策时点期货段误差：未知（UNIDENTIFIED）；决策级验收未通过。** 本次任何测试通过、端点统计稳定，都不改变这一点。
- f237d46 的"期货段误差已实测""包络""均值回复""系统性低估 0.02—0.05 个百分点"及由此引出的总误差和买入判断读法全部撤回。

各项处理见下文；未关闭事项见最后一节。

## P1 · 目标替换：把美股时段端点统计当作亚洲时点误差

**接受。** f237d46 把"前日收盘 → 次日开盘"两个美股现金时段端点之间的基差变化称为"包住估算时刻 t 的区间漂移"，
并据此写"期货段误差已实测，不再是未知"。端点只说明端点：t 落在区间内部，那里没有任何观测，
端点变化小不约束区间内的偏离。亚洲时段官方指数停在昨收，也没有独立的经济公允价值可作参照。

**处理：**

- [acceptance.md](acceptance.md) 第 5 节误差范围恢复为"亚洲决策时点期货段误差：未知；决策级验收未通过"；
  跨日段三日回测改写为样本内结果，不写总体上界；第 6 节新增"亚洲决策时点误差未识别"一条。
- [findings.md](../phase0/futures/findings.md) 第 9 节改名"Yahoo 美股现金时段与跨会话端点基差变化：探索性证据"，
  列出撤回清单；第 10 节明确扩大端点样本不能关闭亚洲误差未知。
- 工具文档字符串与输出文案去掉"包络""实测误差"等用语。

## P1 · 方向推论：符号本身算反了

**接受，且原推导有错。** f237d46 写"隔夜基差收窄 → `F(t)/F(c)` 略微高估指数涨幅 → 估算净值略偏高 → 估算溢价被低估"。
按固定定义 `b(s) = F(s)/I(s) − 1`：

```text
R_F / R_I − 1 = (b(t) − b(c)) / (1 + b(c))
```

基差收窄 `b(t) < b(c)` 给出 `R_F < R_I`：期货**低估**指数涨幅，代理净值偏低，估算溢价**偏高**。
算例：`b(c) = 0.01`、`b(t) = 0.0095` → 相对误差 −4.9505bp；真实溢价 10% 时估算溢价偏高 5.4482bp。

改正符号后也不给出新的方向结论：那几个负值是美股时段端点样本，不是亚洲时点观测。
"亚洲方向为负""系统性低估""亚洲时点溢价被高估"都不写。

## P1 · 总误差与买入结论

**接受。** f237d46 第 9.3 节把跨日段（≤ 0.7bp）、期货段（中位约 3bp、P95 约 5bp）、舍入（0.5bp）并列在 10%—14% 的溢价背景下，
引出"总误差很小（小于 0.1 个百分点）、不会影响买入判断"的读法。不同口径的样本统计不能相加成总体 P95 或上界；
期货段未知时，总误差也未知。9.3 已删除，相关读法撤回。

另撤回"`b(c)` 每天精确已知"：研究序列用的是 Yahoo 五分钟线收盘，既不是正式指数收盘，也不是生产所用的结算锚点
（`core/enav.py` 中"CME 日结算与 16:00 ET 收盘同刻"的注释同步改正，只改文档语义，不改代码）。

## P2 · 端点错配

**接受，并已复现。** f237d46 按标签取期货与指数时间戳的交集，再取每天最后一个共同标签作收盘端点。Yahoo 五分钟线的标签是
区间起点，NDX 在 09-17 还多一条标签为 16:00 的收盘时刻记录，于是那天的"收盘"是 NQ 16:00—16:05 线（29737.25）配
NDX 16:00 记录（29446.98046875），其他日子则是 [15:55, 16:00] 的两条线。把 f237d46 的配对逻辑套在录制夹具上，
复现出 98.57bp，即旧证据中的 `last_close_basis_bp = 98.6`。

**修正（`qdii.core.futures_basis_research`，纯函数；日历调度在工具 I/O 层）：**

- 记录按时间结构分类：普通五分钟线（区间 [ts, ts+300]，观测时刻取区间终点）、指数收盘时刻记录（`SESSION_CLOSE_RECORD`，不进主序列）、
  接收时尚未结束的线（`PARTIAL_BAR`）、不在网格或不在现金会话内的记录（`UNKNOWN`）；值的质量另行判定（null、非数值、非有限、
  非正、同时间戳冲突）。不按价格远近猜记录类型。
- 主序列 `BAR_CLOSE_TO_FIRST_BAR_CLOSE`：前会话最后完整五分钟线收盘 → 下一会话首根五分钟线收盘。09-17 收盘期货价只能是 29744.50，
  NDX 是普通线 29442.1171875。
- NDX 收盘时刻记录只有与独立官方收盘（生产原始日志中的 Nasdaq 29446.98）在公布精度内一致，才标 `OFFICIAL_CLOSE`，
  并且只进单列的 `OFFICIAL_CLOSE_TO_FIRST_BAR_CLOSE` 诊断，配结束于收盘的期货线，不配 16:00—16:05 线。官方记录不足时该诊断为 `NO_DATA`。
- 会话来自现有 `CalendarProvider`（NASDAQ→XNYS），覆盖冬夏令时、提前收盘、节假日；日历不确定时不出样本。
  只在相邻预期会话端点齐全时成对，缺数的会话记为数据缺口，不会把周一到周三标成正常隔夜；零样本返回 `NO_DATA`，不再崩溃。

**测试：** `tests/unit/test_futures_basis_research.py`，夹具 `tests/fixtures/recorded/yahoo_basis_endpoints_20260917.json`
从两条已校验哈希的父响应原样抽取（带父响应 hash 与 msg_id）；合成序列在测试中标 synthetic。
把配对改回按标签（旧行为）会让 10 项测试失败。

## P2 · 均值回复推论

**接受。** 旧盘中统计用重叠窗口，同一段行情被重复计数，各期限覆盖的时段也不同；1 小时与 6 小时的 P95 相近推不出均值回复。
"漂移不随区间增长""均值回复""子区间与整段同量级"均已撤回。

**修正：**

- 分位统一为 nearest-rank（`ceil(p·n) − 1`），输出方法、n 与位次；统计全部用未舍入值，只在展示时舍入。
- 基差变化、`R_F/R_I − 1`、`R_F − R_I` 分字段报告，并用第 1 节恒等式与方向算例（−4.9505bp、+5.4482bp）做测试。
- 盘中窗口改为从首根线收盘起的不重叠固定网格，缺任一内部线即排除、不前向填充，同时报告起点分布；
  另报同一批完整会话、同一开盘起点的 1/2/3/6 小时对比。
- 按交易日整块重采样（固定种子 20260918、默认 2000 次，各期限共用同一批抽样）；有效会话少于 30 个时不给区间。
- 会话内自相关只在同一会话内配对，输入为去日均值的基差水平，常数或样本不足时为 null。
- 输出固定 `asia_decision_error_status = UNIDENTIFIED`、`asia_decision_error_bound_bp = null`、`decision_grade = false`；
  有一项反例测试（端点相差 5bp、区间中途偏离 20bp）锁定"端点统计不产生亚洲界限"。

## 原始证据与重算

**离线重算：**

```text
uv run python tools/futures_basis_intraday.py NQZ26 \
  --raw-file /Users/weizhang/qdii-data/research/raw/yahoo/2026-09-18/06.jsonl \
  --run-id RESEARCH-20260918T062540Z \
  --output reports/mvp/evidence/futures-basis-2026-09-18-v2.json \
  --official-close-root /Users/weizhang/qdii-data \
  --legacy reports/mvp/evidence/futures-basis-2026-09-18.json
```

计划中的命令加了两个可选的只读参数：`--official-close-root` 离线读取 Nasdaq 官方收盘，用于核实 NDX 收盘时刻记录；
`--legacy` 生成逐项对比。`--raw-file` 模式不构造 HTTP 客户端，按 run_id 与端点各取唯一一条响应、校验 body SHA-256，
重复匹配直接拒绝，不取最后一条；输出文件已存在时拒绝覆盖。网络模式先写原始日志再重解析，`finally` 中关闭写入器，
输出文件名带 run_id。测试见 `tests/unit/test_futures_basis_tool.py`（离线无网络、哈希错误、缺响应、重复响应、HTTP/JSON 错误、
空样本、单会话、输出冲突、官方收盘读取与冲突、网络模式先落盘、失败时关闭写入器、同输入同分析）。

**历史证据：** [futures-basis-2026-09-18.json](evidence/futures-basis-2026-09-18.json) 未改动
（SHA-256 `70c6370f4216287d2f43bd2c1d883a252ca1f5c9aeadf43fa368a0675e78aa7f`），**解释上已被取代**（superseded for interpretation）。
新版为 [futures-basis-2026-09-18-v2.json](evidence/futures-basis-2026-09-18-v2.json)。

**新旧数值差异（均来自 v2 JSON 的 `legacy_comparison`）：**

| 项 | v1（f237d46） | v2 | 变化来源 |
|---|---:|---:|---|
| 跨会话样本 | 21 | 21 | 无：工具按旧规则复现全部 21 个值，两版端点记录相同 |
| 绝对值中位 | 2.8bp | 2.85bp | 精度（v1 先舍入到 0.1bp）；n=21 为奇数，两种中位数算法取同一位置 |
| 绝对值 P95 | 4.9bp | 4.88bp（第 20/21 个） | 精度；位次相同（`int(0.95·21) = ceil(0.95·21) − 1 = 19`） |
| 绝对值最大 | 5.4bp | 5.43bp | 精度 |
| 最后一天收盘基差 | 98.6bp | 102.70bp（普通线）；101.04bp（已核实正式收盘诊断） | 端点：v1 用 16:00—16:05 期货线配收盘时刻记录 |
| 供应商对照 | +1.9bp | −0.50bp（新浪昨结算 29743.000 相对结束于 16:00 的线 29744.50） | 端点；两者定义不同，不是供应商误差上界 |
| 盘中 1/2/3/6 小时 | 重叠窗口 1339/1088/849/128 个 | 不重叠窗口 96/43/25/9 个 | 方法改变（重叠→不重叠、缺线排除），**不可比** |
| 盘中 P95 | 5.8/5.9/5.8/6.5bp | 6.18/6.71/3.18/3.59bp | 同上；覆盖的会话与时段各不相同 |
| 同起点对比 | 无 | 9 个会话 | 新增；样本过少，不支持任何增长或不增长的说法 |
| 置信区间 | 无 | 不给出（22 < 30 个会话） | 新增门槛 |

21 个跨会话样本不变，是因为 v1 的问题不在这 21 对，而在最后一天的端点诊断和盘中窗口的读法。数值没变，
不等于当时的结论成立：它们依旧只是美股端点样本，不是亚洲时点误差。
另有一处观察：同一 Yahoo 响应的 `meta.previousClose` 也是 29743.0，与新浪昨结算相同。两家供应商字段一致，仍不等于官方结算确认。
